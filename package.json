{
  "name": "prompter-extension",
  "version": "1.0.0",
  "description": "Chrome extension for formatting text with AI templates",
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run build:dev",
    "build:dev": "vite build --mode development && npm run build:content",
    "build:production": "npm run clean && npm run build:prod",
    "build:prod": "vite build --mode production && npm run build:content",
    "build:content": "node scripts/build-content.js",
    "build:vite": "vite build",
    "dev": "npm run build && npm run watch",
    "watch": "node scripts/watch.js",
    "clean": "rimraf dist/",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "test": "jest",
    "validate": "npm run lint && npm run test"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "chokidar": "^3.5.0",
    "esbuild": "^0.19.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "postcss": "^8.4.0",
    "rimraf": "^5.0.0",
    "tailwindcss": "^4.1.11",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@tailwindcss/postcss": "^4.1.11",
    "insert-text-at-cursor": "^0.3.0",
    "json-stringify-pretty-compact": "^4.0.0",
    "lucide": "^0.535.0"
  },
  "files": [
    "src/",
    "dist/",
    "manifest.json",
    "popup.html",
    "popup.css"
  ],
  "keywords": [
    "chrome-extension",
    "ai",
    "text-formatting",
    "templates",
    "productivity"
  ],
  "author": "Prompter Extension Team",
  "license": "MIT"
}
